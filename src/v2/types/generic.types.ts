export interface StatsMetrics {
    renderTotal: number;
    renderNodes: number;
    renderLinks: number;
    renderLinkLabels: number;
    renderNodeLabels: number;
    styleResolution: number;
    hoverChecks: number;
    canvasCalls: number;
    frameCount: number;
  }